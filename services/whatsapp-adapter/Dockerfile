# WhatsApp Adapter (whatsapp-adapter) — Dockerfile (placeholder Fase 0).
# El stack concreto (lenguaje/runtime) se decidirá en un ADR antes de
# desarrollar el servicio. Este archivo documenta el contrato de empaquetado:
#
#   - Exponer el puerto SERVICE_PORT (ver .env.example).
#   - Leer configuración SOLO desde variables de entorno / secret manager.
#   - No incorporar secretos en la imagen.
#   - Endpoint /api/v1/health disponible para smoke tests.
#
# FROM <runtime-a-definir>
# WORKDIR /app
# COPY . /app
# CMD ["<comando-de-arranque>"]
